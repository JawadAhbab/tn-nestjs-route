type Props = {
    site: string;
    cdn?: string;
    loggerImport?: string;
    loggerMethod?: string;
};
export declare const templateBasics: ({ site, cdn, loggerImport, loggerMethod }: Props) => string;
export {};
